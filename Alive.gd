extends Sprite

func _ready():
	#position = Vector2(get_viewport().size.x/2,get_viewport().size.y/2)
	#position.x=100
	#position.y=100
	#print("X = "+str(position.x)+" Y = "+str(position.y))
	#print("get size = "+str(texture.get_size()))
	